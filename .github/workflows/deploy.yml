name: Talent Metric CI/CD Pipeline

on:
  workflow_dispatch:
  push:
    branches: [main, dev]

permissions:
  id-token: write
  contents: write

env:
  AWS_REGION: us-east-1
  AWS_ACCOUNT_ID: 382625484581
  IAM_ROLE_NAME: Github-acition-oicd            
  BACKEND_REPOSITORY: talent-metric-backend
  FRONTEND_REPOSITORY: talent-metric-frontend

jobs:
  build:
    name: "🐳 Build & Push to ECR"
    runs-on: ubuntu-latest
    outputs:
      image_tag: ${{ steps.meta.outputs.image_tag }}
      ecr_registry: ${{ steps.login-ecr.outputs.registry }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::${{ env.AWS_ACCOUNT_ID }}:role/${{ env.IAM_ROLE_NAME }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Set image tag (git SHA)
        id: meta
        run: echo "image_tag=${{ github.sha }}" >> $GITHUB_OUTPUT

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build & push Backend
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          file: ./backend/Dockerfile
          push: true
          tags: |
            ${{ steps.login-ecr.outputs.registry }}/${{ env.BACKEND_REPOSITORY }}:${{ steps.meta.outputs.image_tag }}
            ${{ steps.login-ecr.outputs.registry }}/${{ env.BACKEND_REPOSITORY }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
          provenance: false

      - name: Build & push Frontend
        uses: docker/build-push-action@v5
        with:
          context: ./frontend
          file: ./frontend/Dockerfile
          push: true
          tags: |
            ${{ steps.login-ecr.outputs.registry }}/${{ env.FRONTEND_REPOSITORY }}:${{ steps.meta.outputs.image_tag }}
            ${{ steps.login-ecr.outputs.registry }}/${{ env.FRONTEND_REPOSITORY }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
          provenance: false

  deploy_dev:
    name: " Deploy → talent-metric-dev"
    runs-on: ubuntu-latest
    needs: build
   # if: github.event.pull_request.merged == true
    environment: development
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          repository: AthivaTechnology/athiva-eks-manifests  
          token: ${{ secrets.MANIFEST_REPO_TOKEN }}
          ref: main

      - name: Update image tags in dev manifest
        env:
          REGISTRY: ${{ needs.build.outputs.ecr_registry }}
          TAG: ${{ needs.build.outputs.image_tag }}
        run: |
          sed -i "s|image: .*${{ env.BACKEND_REPOSITORY }}.*|image: $REGISTRY/${{ env.BACKEND_REPOSITORY }}:$TAG|g" talent-metric/k8s/dev.yaml
          sed -i "s|image: .*${{ env.FRONTEND_REPOSITORY }}.*|image: $REGISTRY/${{ env.FRONTEND_REPOSITORY }}:$TAG|g" talent-metric/k8s/dev.yaml
          # Replace general placeholders
          sed -i "s/<YOUR_ACCOUNT_ID>/${{ secrets.AWS_ACCOUNT_ID }}/g" talent-metric/k8s/dev.yaml
          sed -i "s/<YOUR_REGION>/${{ env.AWS_REGION }}/g" talent-metric/k8s/dev.yaml


      - name: Commit & push updated manifest
        env:
          TAG: ${{ needs.build.outputs.image_tag }}
        run: |
          git config user.name "talent-metric-ci-bot"
          git config user.email "ci@talent-metric.com"
          git add talent-metric/k8s/dev.yaml
          git diff --staged --quiet && echo "No manifest changes, skipping commit" && exit 0
          git commit -m "ci(dev): update images to ${TAG:0:7} [skip ci]"
          git push origin main

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::${{ env.AWS_ACCOUNT_ID }}:role/${{ env.IAM_ROLE_NAME }}
          aws-region: ${{ env.AWS_REGION }}

  deploy_prod:
    name: "Deploy → talent-metric-prod [approval required]"
    runs-on: ubuntu-latest
    needs: [build,deploy_dev]
    if: github.ref == 'refs/heads/main'
#   environment: production               
    steps:
      - name: Checkout
        uses: actions/checkout@v4 
        with:
          repository: AthivaTechnology/athiva-eks-manifests  
          token: ${{ secrets.MANIFEST_REPO_TOKEN }}
          ref: main

      - name: Update image tags in prod manifest
        env:
          REGISTRY: ${{ needs.build.outputs.ecr_registry }}
          TAG: ${{ needs.build.outputs.image_tag }}
        run: |
          sed -i "s|image: .*${{ env.BACKEND_REPOSITORY }}.*|image: $REGISTRY/${{ env.BACKEND_REPOSITORY }}:$TAG|g" talent-metric/k8s/prod.yaml
          sed -i "s|image: .*${{ env.FRONTEND_REPOSITORY }}.*|image: $REGISTRY/${{ env.FRONTEND_REPOSITORY }}:$TAG|g" talent-metric/k8s/prod.yaml
          # Replace general placeholders
          sed -i "s/<YOUR_ACCOUNT_ID>/${{ secrets.AWS_ACCOUNT_ID }}/g" talent-metric/k8s/prod.yaml
          sed -i "s/<YOUR_REGION>/${{ env.AWS_REGION }}/g" talent-metric/k8s/prod.yaml

      - name: Commit & push updated manifest
        env:
          TAG: ${{ needs.build.outputs.image_tag }}
        run: |
          git config user.name "talent-metric-ci-bot"
          git config user.email "ci@talent-metric.com"
          git add talent-metric/k8s/prod.yaml
          git diff --staged --quiet && echo "No manifest changes, skipping commit" && exit 0
          git commit -m "ci(prod): update images to ${TAG:0:7} [skip ci]"
          git push origin main

      
      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::${{ env.AWS_ACCOUNT_ID }}:role/${{ env.IAM_ROLE_NAME }}
          aws-region: ${{ env.AWS_REGION }}